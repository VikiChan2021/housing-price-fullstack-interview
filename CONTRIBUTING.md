# Contributing

## 开始前

1. 阅读 `AGENTS.md` 和 `docs/INDEX.md`。
2. 选择实施路线中的一个阶段。
3. 在任务描述中引用需求 ID 和验收 ID。
4. 确认不会修改 `data/raw/` 或 `references/original/`。

## 变更要求

- 功能、测试和相关文档应在同一变更中保持一致。
- 新架构决策使用 ADR。
- API 变化同步 OpenAPI、生成类型和契约测试。
- PR/提交说明列出执行过的命令和未验证边界。

## Review 清单

- 是否满足对应需求，而不是只完成 happy path？
- 是否增加了合适层级的测试？
- 是否处理下游超时、无效输入和空状态？
- 是否没有引入秘密、绝对路径或不必要依赖？
- 是否更新项目状态和追踪矩阵？
- 可见 UI 是否经过真实浏览器检查？

