# AGENTS.md

本文件适用于整个仓库。任何 AI 或开发者开始实现前必须先阅读根目录 `README.md` 和 `docs/INDEX.md`。

## 当前阶段

- 当前仓库是文档与目录骨架。
- 没有业务代码，没有可运行服务，也没有已经通过的自动化或浏览器验收。
- 文档中的架构、API 和测试均是待实现规范，不是现状描述。

## 事实优先级

发生冲突时按以下顺序处理：

1. `references/original/Interview Tasks Fullstack.pdf` 中的面试官原始要求。
2. `docs/requirements/` 中的需求和验收标准。
3. 已接受的 `docs/adr/` 架构决策。
4. 其他设计文档。
5. 代码中的临时实现选择。

不得静默降低原题要求。如需改变范围，先新增 ADR 并更新追踪矩阵。

## 实现边界

- 保持 `data/raw/` 和 `references/original/` 原样，不覆盖、不重写。
- `id` 只用于标识，不得作为模型特征。
- CSV 读取必须处理 UTF-8 BOM，推荐 `utf-8-sig`。
- 模型训练与 API 服务分离；请求时不得重新训练模型。
- `ml-api` 必须支持单条和批量预测，并提供模型信息与健康检查。
- `estimator-api` 和 `market-api` 均通过 HTTP 调用 `ml-api`，不得复制模型推理逻辑。
- 不在浏览器代码或仓库中保存密钥。
- 不为本题引入非必要基础设施；历史记录默认使用浏览器本地存储，Java 缓存默认使用 Caffeine。

## 质量要求

- 每个阶段都要实现相应单元测试、契约测试或集成测试。
- 服务启动成功不等于完成；可见 UI 必须经过真实浏览器验收。
- 将“计划”“已实现”“本地验证”“公网验证”分开记录。
- 更新接口、环境变量或架构时，同步更新相关文档。
- 错误响应必须稳定、可读，并包含可用于排查的请求标识。
- 记录模型版本、特征顺序、训练数据哈希和评估指标。

## 开发顺序

遵循 `docs/development/IMPLEMENTATION_ROADMAP.md`。除非有明确理由，不要同时铺开四个应用。先固定 API 契约和模型产物格式，再逐层集成。

## 完成定义

只有满足 `docs/requirements/ACCEPTANCE_CRITERIA.md` 并完成 `docs/testing/TEST_STRATEGY.md` 规定的验证，才可声明项目完成。若某项未执行，必须明确标记为未验证。

