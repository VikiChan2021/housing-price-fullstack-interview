# 项目状态

最后更新：2026-08-14

## 已完成

- 创建独立 Git 仓库与 `main` 分支。
- 归档面试官原始 PDF、训练 CSV 和预测 CSV。
- 记录原始资料的 SHA-256。
- 创建应用、服务、测试、基础设施和文档目录骨架。
- 完成需求、架构、接口、测试、部署和演示文档的开发前审计。
- 接受 ADR-004，冻结核心工具链/直接依赖与 lockfile 策略。
- 固定 API 输入边界和可复现模型评估协议。

## 尚未开始

- Python 模型训练代码。
- FastAPI `ml-api`。
- Python `estimator-api`。
- Java/Spring Boot `market-api`。
- Next.js Portal。
- Dockerfile 与 Docker Compose。
- 自动化测试、浏览器验收和公网部署。

## 已有探索证据，不等于最终模型结果

对原始 50 条训练数据做过只读探索：数据无缺失、无重复；特征高度相关；普通线性回归可以获得较高的交叉验证 R²。正式项目必须通过仓库内可复现训练脚本重新计算并持久化指标，不能直接复制探索数字作为最终结果。

## 下一步

等待仓库所有者批准。批准后执行实施路线 Phase 0B：创建最小工程脚手架与 lockfile、生成机器可校验的契约基线并验证工具链。未获批准前不开始正式代码开发。
