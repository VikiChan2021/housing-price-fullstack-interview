# End-to-End Tests

实现后使用真实浏览器验证 Docker Compose 中的完整调用链。

最低覆盖：

- Estimator 成功、校验失败、历史、比较、ML 不可用。
- Market 初始加载、筛选、排序、what-if、CSV/PDF 导出。
- 共享导航、loading、error、移动视口和控制台/network 检查。

测试用例必须引用 `docs/requirements/ACCEPTANCE_CRITERIA.md` 的 AC 编号。

